# Cloudflare Workers build optimization
# Skip optional dependencies (like sharp platform-specific binaries)
optional=false

# Skip installing development dependencies in production builds
# This will be overridden by Cloudflare if needed
production=false
