# Cloudflare Workers build optimization
# Skip optional dependencies (like sharp platform-specific binaries)
optional=false

# Use npm instead of bun for better OpenNext compatibility
# This ensures consistent builds across environments
